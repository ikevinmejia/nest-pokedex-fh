version: '4.86.0'

services:
  db:
    image: mongo:8.0.15
    restart: always
    ports:
      - 27017:27017
    environment:
      MONGODB_DATABASE: nest-pokemon
    volumes:
      - ./mongo:/data/db
